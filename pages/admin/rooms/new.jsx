import { NewRoomPage, Layout } from "@core-ui/core-uiIndex";

const NewRoom = () => {
  return (
    <Layout title="Create New Room">
      <NewRoomPage />
    </Layout>
  );
};

export default NewRoom;
