import { ResetPasswordPage } from "@core-ui/core-uiIndex";
import { Layout } from "@core-ui/core-uiIndex";

const ResetPassword = () => {
  return (
    <Layout title="Reset Password">
      <ResetPasswordPage />
    </Layout>
  );
};

export default ResetPassword;
